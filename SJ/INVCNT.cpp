#include<iostream>
#include<queue>
#include<vector>
#include<algorithm>
using namespace std;
int main()
{
    typedef long long ll;
    ll q;cin>>q;
    while(q--)
    {
        ll n,t;cin>>n;
        queue<ll> v;
        while (n--)
        {   cin>>t;v.push(t);}
        ll ans=0;
        while(!v.empty())
        {

        }
        cout<<ans<<"\n";
    }
}